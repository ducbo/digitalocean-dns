# Example file for sync_dns
# Copy this file to "sync_dns_settings.py" and adjust with your values.
# It's in .gitignore so won't be synced

ip="1.1.1.1"
auth_token=""
bindfolder = "/root/digitalocean-dns-sync/"
bindextension = ".zone"
